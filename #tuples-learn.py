#tuples-learn
#tuples are immutables which is better when you need to use constant data through the program.

samer = ('Kani', 'Ahmed', 'Arya')
print ('your data type is', type(samer))

print (samer[0])

samer[0]= '15'  #this line will not make change to the tuples (immutable)